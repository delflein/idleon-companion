<script setup>
// STUB (M5-prep) — renders the shared PendingPage until LandRank is ported.
// A batch agent rewrites ONLY this file into the real port; router.js and App.vue never change
// (the route + nav entry + title/icon meta already exist in router.js's PAGES array).
import PendingPage from "./PendingPage.vue";
</script>

<template>
  <PendingPage />
</template>
