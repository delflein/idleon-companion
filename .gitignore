# Account access — refreshToken grants full read of the IdleOn save. Never commit.
.idleon-auth.json

# Personal save data & derived history
idleon-export*.json.gz
idleon.db
idleon.db-shm
idleon.db-wal
savegame.json
savedata.js
history.json
history.js

# OS / editor
.DS_Store
node_modules/
companion.log

# Game client (LavaFlame2 copyright) — ground-truth formulas, grep-only. NEVER commit.
N.js
idleon_index.html

# IdleonToolbox reference clone (MIT, github.com/Morta1/IdleonToolbox) — used as an INDEX of
# which terms feed a stat and for lifting data tables; formulas verified in N.js before trust.
idleon-toolbox/

# SPA/PWA build output (docs/ARCHITECTURE.md) — .github/workflows/deploy.yml builds this fresh.
dist/
