/* gamedata-w3-towers.mjs — Construction/Towers (World 3, Frostbite Tundra) static tables,
 * extracted verbatim from N.js. GENERATED by a one-off extraction script (scratchpad, not
 * committed); refresh after a major patch.
 *
 * SOURCE TABLE: na.TowerInfo=function(){return[...]} (N.js:24049, closing `]};` on N.js:24058)
 * returns ONE 27-row array shared by three unrelated UI screens — confirmed by index-offset
 * reads elsewhere in the client (`TowerInfo[9+b]` for Worship pillars in
 * p._customBlock_TowerStats N.js:12219-12229 and m._customBlock_AtomCollider N.js:17840;
 * `TowerInfo[b+18]` for Worship shrines in "ShrineExpBonus" N.js:12274-12275):
 *   rows  0- 8 (9 rows) : Construction TOWERS        <- THIS FILE's scope
 *   rows  9-17 (9 rows) : Worship PILLARS/totems (Pulse_Mage..Voidinator)  — out of scope
 *   rows 18-26 (9 rows) : Worship SHRINES (Woodular_Shrine..Primordial_Shrine) — out of scope
 *     (na.ShrineInfo(), N.js:24075, is a SEPARATE, differently-shaped table also read by the
 *     Worship shrine UI — the two shrine data sources coexist in the client; not investigated
 *     further since shrines are outside this file's Construction-tower scope.)
 * The game attribute "TowerInfo" (no `CustomLists` prefix) is the SAVE array of the player's
 * current level per row (same 0..26 indexing) — every formula below reads BOTH the static
 * `CustomLists.TowerInfo[i]` row (this file) and the live `TowerInfo[i]` level from the save.
 *
 * ATOM COLLIDER IS A TOWER ROW: row 8 ("Atom_Collider") lives in this same array, built and
 * leveled exactly like the other 8 Construction towers (same Refinery-salt cost mechanic, same
 * max-level/cost-growth fields). Its own per-level effect ("Upgrade costs for atoms are {%
 * cheaper") is transcribed below in TOWER_INFO row 8; the ATOM cost-curve it discounts lives in
 * gamedata-w3-atoms.mjs (cross-reference: AtomCost's discount factor includes `TowerInfo[8]/10`
 * as a percent, N.js:17843 — i.e. Atom_Collider's own level, /10, is a flat % atom-cost discount,
 * NOT the "every 10 lvs" stepped mechanic the tooltip text implies).
 *
 * ROW FIELDS — each row is `Name Desc x1 x2 RefineryMat Blank matAmountPerLevel 0 maxLevel
 * costGrowthRate field10` after `.split(" ")` (Desc has embedded `_`/`@`/`{` placeholders, no
 * literal spaces, so it stays one token). Field semantics below were reverse-engineered from the
 * consumer functions (NOT guessed) and cross-checked field-for-field against idleon-toolbox's
 * data/website-data.json `towers` object — 0 mismatches on all 9 rows' maxLevel/costGrowthRate/
 * itemReq.amount/itemReq.rawName. Toolbox mislabels field[9] "bonusInc" and fields[2,3] "costInc"
 * — per this repo's cross-check policy toolbox names aren't trusted; the names below reflect
 * what N.js's own switch-statement dispatchers (WorkbenchStuff/TowerStats) actually DO with each
 * field, cited inline:
 *   [2] x1              — linear per-level BONUS coefficient (the tooltip's "+{%_Effect" number
 *                          IS `x1 * level`, no decay/curve — confirmed for row 1 Library, whose
 *                          live BookReqTime formula in gamedata-w3-library.mjs uses `5*TowerInfo[1]`
 *                          verbatim, matching this row's x1=5 exactly).
 *   [3] x2Unconfirmed    — present in every row (30/50/1/1.4 etc.) but NOT read by any Construction
 *                          -tower consumer function found (TowerStats/Tower2Stats's "atkcd"/"range"
 *                          arms that DO read field[3]-as-x2 only fire for b=9..17, the Worship
 *                          pillars using the SAME array — see file header). For rows 0-8 this
 *                          field's purpose is UNCONFIRMED; kept verbatim, not guessed at.
 *   [4] refineryMat      — which Refinery-tier salt (Refinery1..Refinery6, gamedata-w3-atoms.mjs's
 *                          sibling salt system) is spent to START an upgrade.
 *   [5] "Blank"          — literal filler token in every row, confirmed unused.
 *   [6] matAmountPerLevel— the SALT/resource cost coefficient. Live formula "TowerSaltCost"
 *                          (p._customBlock_WorkbenchStuff, N.js:12267) — see FORMULAS below.
 *   [7] always "0"       — literal 0 in all 9 (in fact all 27) rows. The "TowerMatCost" dispatcher
 *                          arm (N.js:12267-12269) multiplies BY this field, so that formula always
 *                          evaluates to 0 for every tower — almost certainly DEAD/superseded code
 *                          (TowerSaltCost, whose field[6] is always nonzero, appears to be the
 *                          live cost path). Flagged, not silently dropped.
 *   [8] maxLevel         — the tower's max level BEFORE extension bonuses (Carbon atom's
 *                          "+{_max_levels" to "wizard towers", etc. — those are Worship-pillar-
 *                          only per Carbon's own tooltip, so Construction towers 0-8 are believed
 *                          unaffected by that particular extension; not independently verified).
 *                          Exact match vs toolbox `maxLevel` on all 9 rows.
 *   [9] costGrowthRate   — the exponential base in the TowerBuildReq cost curve (below). Exact
 *                          match vs toolbox `bonusInc` (toolbox's name for this field, which this
 *                          file does NOT reuse since the field never appears in a *bonus* formula
 *                          for rows 0-8, only in the build-cost curve).
 *   [10] field10Unconfirmed — equals maxLevel [8] for rows 2,4,5,7,8 but diverges for rows 0,1,3,6
 *                          (10 vs 11; 201 vs 101; 10 vs 11; 50 vs 15). No consumer of `TowerInfo[i][10]`
 *                          was found in N.js for i in 0..8 — kept verbatim, UNVERIFIED.
 */

export const TOWER_INFO = [{"id":0,"name":"3D_Printer","desc":"Using_the_new_Star_Talent_(on_the_2nd_tab_of_Star_Talents),_you_can_collect_samples_to_start_printing_resources!_@_Current_Bonuses:_@_$_Player_Slots_Unlocked","x1":1,"x2Unconfirmed":30,"refineryMat":"Refinery1","matAmountPerLevel":3,"maxLevel":11,"costGrowthRate":1,"field10Unconfirmed":10},{"id":1,"name":"Talent_Book_Library","desc":"Relive_your_youth_by_checking_out_books,_and_further_relive_your_youth_by_never_returning_them!_Instead,_use_them_to_boost_your_talent_max_levels._@_Current_Bonuses:_@_+{%_Checkout_Refresh_Speed","x1":5,"x2Unconfirmed":30,"refineryMat":"Refinery1","matAmountPerLevel":6,"maxLevel":101,"costGrowthRate":1.18,"field10Unconfirmed":201},{"id":2,"name":"Death_Note","desc":"Defeat_TONS_of_monsters_to_boost_your_Multikill_Rate_for_each_world._Upgrading_this_tower_also_boosts_your_base_Multikill_Bonus_in_all_worlds._@_Current_Bonuses:_@_+{%_Multikill_Bonus","x1":2,"x2Unconfirmed":50,"refineryMat":"Refinery2","matAmountPerLevel":10,"maxLevel":51,"costGrowthRate":1.23,"field10Unconfirmed":51},{"id":3,"name":"Salt_Lick","desc":"Spend_refinery_salts_and_other_World_3_Resources_in_return_for_bonuses_from_the_hungry_blobulytes!_@_Current_Bonuses:_@_$_Available_Upgrades","x1":1,"x2Unconfirmed":30,"refineryMat":"Refinery2","matAmountPerLevel":20,"maxLevel":11,"costGrowthRate":2,"field10Unconfirmed":10},{"id":4,"name":"Chest_Space","desc":"Just_gives_more_Storage_Chest_slots,_straight_up._I_gotchu_dawg,_yeh._(Passive_Upgrade)_@_Current_Bonuses:_@_+2_Storage_Chest_Slots_@_+{_more_Storage_Chest_Slots","x1":2,"x2Unconfirmed":30,"refineryMat":"Refinery2","matAmountPerLevel":25,"maxLevel":25,"costGrowthRate":1.27,"field10Unconfirmed":25},{"id":5,"name":"Cost_Cruncher","desc":"Reduces_the_resource_costs_of_upgrading_buildings._Doesn't_affect_'building'_phase_cost_(Passive_Upgrade)_@_Current_Bonuses:_@_-10%_Resource_Cost_@_-{%_more_Resource_Cost","x1":1,"x2Unconfirmed":30,"refineryMat":"Refinery3","matAmountPerLevel":20,"maxLevel":60,"costGrowthRate":1.106,"field10Unconfirmed":60},{"id":6,"name":"Trapper_Drone","desc":"Remotely_deploy_AND_collect_traps_to_and_from_any_discovered_critter_location!_Collect-all_is_based_on_Hunter's_Eagle_Eye_Talent._@_Current_Bonuses:_@_+{%_Extra_critters_from_all_traps","x1":10,"x2Unconfirmed":1,"refineryMat":"Refinery3","matAmountPerLevel":30,"maxLevel":15,"costGrowthRate":1.95,"field10Unconfirmed":50},{"id":7,"name":"Automation_Arm","desc":"Automate_some_of_the_more_tedious_actions_with_the_flip_of_a_switch!_Let_the_robots_do_the_dirty_work!_@_Current_Bonuses:_@_+{_Automated_System,_controlled_via_@_the_Robotic_Arm.","x1":1,"x2Unconfirmed":1,"refineryMat":"Refinery3","matAmountPerLevel":50,"maxLevel":5,"costGrowthRate":4,"field10Unconfirmed":5},{"id":8,"name":"Atom_Collider","desc":"Accelerate_enough_material_to_high_speeds,_and_you_can_create_the_first_physical_atoms_in_this_pixellated_world!_@_Current_Bonuses:_@_Upgrade_costs_for_atoms_are_{%_cheaper_(Increases_every_10_lvs).","x1":0.1,"x2Unconfirmed":0.1,"refineryMat":"Refinery6","matAmountPerLevel":10,"maxLevel":200,"costGrowthRate":1.14,"field10Unconfirmed":200}];

/* FORMULAS — all from p._customBlock_WorkbenchStuff (N.js:12230-12279) unless noted. `b` is the
 * tower row index (0-8 for this file's scope), `TowerInfo[b]` (no CustomLists prefix) is the
 * player's CURRENT LEVEL of tower b from the save.
 *
 * TowerBuildReq(b) — N.js:12266. The BUILD-POINT progress threshold to finish the level the
 * tower is currently mid-upgrading (accumulated via PlayerBuildSpd below, NOT paid instantly):
 *   if (b == 0)  return 2 * Math.pow(TowerInfo[0]+1, 2) * Math.pow(1.3, TowerInfo[0]+1);
 *                  // 3D_Printer is hardcoded specially; RANDOlist[13][0] (=3) is dead/unused
 *                  // for this row because of the override.
 *   else         return RANDOlist[13][b] * Math.pow(CustomLists.TowerInfo[b][9], TowerInfo[b]);
 *                  // RANDOlist[13] (ka.RANDOlist, N.js:23545) rows 0-8 (out of its own 27):
 *                  //   [3, 200, 2250, 12000, 25000, 60000, 100000, 150000, 50000000]
 *                  // (index 0's value of 3 is the unused-due-to-override one above)
 *
 * TowerSaltCost(b) — N.js:12267. The Refinery-salt cost (of TOWER_INFO[b].refineryMat, quantity
 * scaled off TOWER_INFO[b].matAmountPerLevel) to START the next level's build, discounted by
 * Cost_Cruncher's (row 5) own level and x1:
 *   costCruncherLv = TowerInfo[5];
 *   discount = Math.max(0.2, 1 - (Math.min(0.1, 0.1*Math.floor((TowerInfo[5]+999)/1000))
 *                                  + Math.max(0, costCruncherLv-1) * TOWER_INFO[5].x1/100));
 *   cost = Math.floor(discount * TOWER_INFO[b].matAmountPerLevel * (TowerInfo[b] + 1));
 *
 * TowerMatCost(b) — N.js:12267-12269. Present in the dispatcher but multiplies by
 * CustomLists.TowerInfo[b][7], which is literally "0" in every row (see field docs above) —
 * ALWAYS EVALUATES TO 0 for every tower. Transcribed for completeness, flagged as apparently dead:
 *   Math.floor(Math.min(1e7, discount(same as TowerSaltCost) * TOWER_INFO[b].field7(=0)
 *     * Math.pow(TOWER_INFO[b].costGrowthRate + 0.03
 *         - (TOWER_INFO[b].costGrowthRate + 0.03 - 1.05) * TowerInfo[b] / (TOWER_INFO[b].maxLevel/2 + TowerInfo[b]),
 *       TowerInfo[b])));
 *
 * PlayerBuildSpd(charIdx) — N.js:12254-12258, the per-character BUILD-RATE formula (feeds
 * TowerBuildReq's progress meter, presumably per-hour or per-tick — rate UNITS not confirmed from
 * this dispatcher alone). Verbatim (charIdx >= 0 branch, i.e. a real player, not "total/summed"):
 *   f = 3 * Math.pow(Lv0[8]/2 + 0.7, 1.6);                          // Lv0[8] = Construction skill level
 *   g = 1 + Lv0[8] * AlchBubbles.Construction / 100;
 *   M = 1 + (StampBonusOfTypeX("BuildProd")
 *          + 0.25*PostOfficeInfo[17][0] + GuildBonuses(5) + EtcBonuses("30")
 *          + Math.min(5, 5*AchieveStatus(153)) + WorkbenchStuff("ConstMasteryBonus",2,0)
 *          + AlchVials.Contspd + ArcadeBonus(44)
 *          + Summoning("VotingBonusz",18,0)
 *          + Summoning("VaultUpgBonus",48,0)*Summoning("VaultKillzTotal",11,0)
 *          + Bubbastuff("BubbaRoG_Bonuses",1,0)) / 100;
 *   l = (1 + Summoning("WinBonus",13,0)/100) * (1 + GamingStatType("PaletteBonus",25,0)/100)
 *         * (1 + AlchVials["6turtle"]/100);
 *   [t,u,v] = TalentDescriptions[131][1][2,0,1];                    // Talent #131 "func,x1,x2"
 *   return f * g * M * l * (1 + ArbitraryCode5Inputs(t,u,v, SkillLevels[131], 0, 0)
 *            * (AtomCollider("AtomBonuses",1,0) + getLOG(ItemsAndStorageOWNED.Refinery1)) / 100);
 *   // ArbitraryCode5Inputs is the SAME shared growth evaluator documented in gamedata-stamps.mjs.
 *   // AtomBonuses(1,..) = Helium atom's bonus (see gamedata-w3-atoms.mjs); getLOG is a log-scale
 *   // helper (definition not chased down — out of this file's scope).
 *
 * FLAG PLACEMENT COST ("FlagReq", N.js:12230):
 *   if (flagIdx < 96) return 25 * FLAG_REQS[flagIdx];
 *   else return 5e6 * (1 + Math.min(9, 9*Math.round(flagIdx-96)) + Math.max(0, 10*Math.round(flagIdx-97)))
 *              * Math.pow(4, Math.round(flagIdx-96));
 * FLAG_REQS (Ya.FlagReqs, N.js:24394) is the raw 96-entry per-slot multiplier table below; the
 * live cost is 25x that (currency: Flaggy, per idleon-toolbox's `flagsReqs` naming — 0 mismatches,
 * all 96 values exact match). Flags 96+ (the two 12-slot "extra columns" left/right of the main
 * 12x8 board, per idleon-toolbox construction.ts LEFT_FLAG_INDEX=96/RIGHT_FLAG_INDEX=108) use the
 * closed-form overflow formula instead of a table lookup.
 */
export const FLAG_REQS = [10000000,2000000,700000,12000,4000,1250,1250,4000,12000,700000,2000000,10000000,2000000,40000,7500,350,60,25,25,60,350,7500,40000,2000000,700000,12500,600,9,1,1,1,1,9,600,12500,700000,100000,2000,130,4,1,1,1,1,4,130,2000,100000,100000,2000,130,4,1,1,1,1,4,130,2000,100000,700000,12500,600,9,1,1,1,1,9,600,12500,700000,2000000,40000,7500,350,60,25,25,60,350,7500,40000,2000000,10000000,2000000,700000,12000,4000,1250,1250,4000,12000,700000,2000000,10000000];

/* COG STAT SEMANTICS — La.CogMapKeys (N.js:24601-24602), the letter->effect-description table for
 * cogs placed on the Construction board. 11 keys (NOT 4 — a cog's "Number2Letter" letter-code
 * indexes this table), 3 unused/troll entries kept verbatim:
 *   a = "_Build_Rate/HR"        (flat, per-hour)      f = "%_Player_Construct_XP"
 *   b = "_Construct_Exp/HR"     (flat, per-hour)      g = "%_Flaggy_Rate"
 *   c = "_Flaggy_Rate/HR"       (flat, per-hour)      h = "_"   (blank/unused)
 *   d = "%_Bonus_Construct_EXP"                       i = "_"   (blank/unused)
 *   e = "%_Build_Rate"                                j = "%_Speed_to_Flags"
 *                                                      k = "%_Nothing_sowwy" (troll/unused)
 * Exact match vs idleon-toolbox `cogKeyMap` (0 mismatches, all 11 keys).
 * La.CogMapSurroundDisp (N.js:24602, same statement) is the SEPARATE board-geometry-pattern table
 * (adjacent/diagonal/row/column/etc. -> single-char UI glyph) — not a stat, not transcribed here
 * as it carries no numeric game data.
 *
 * COG TIER UNLOCK COST ("CogBuildReq"/"CogBuildClaims", N.js:12252-12253) — a SEPARATE "4" from
 * the 11 CogMapKeys above: cogs come in 4 size/rarity TIERS (tier index 0-3, looped `for(b=0;4>b;)`
 * at N.js:13999/14072/14491), each with its own Flaggy-funded progress meter (save slot
 * `TowerInfo[62+tier]`) that unlocks another cog slot of that tier every time it crosses a
 * threshold:
 *   CogBuildReq(tier, claimsSoFar) = claimsSoFar < 0 ? 0
 *     : COG_TIER_BASE_COST[tier] * Math.pow(2 + tier, claimsSoFar);
 *   CogBuildClaims(tier) = the largest claimsSoFar such that TowerInfo[62+tier] >= CogBuildReq(tier, claimsSoFar)
 *                           (walked iteratively in N.js, capped at 100 — see source for the loop).
 * COG_TIER_BASE_COST = CustomLists.RANDOlist[12] (N.js:23545 region) — exact match vs toolbox
 * `randomList[12]`.
 */
export const COG_STAT_TYPES = {"a":"_Build_Rate/HR","b":"_Construct_Exp/HR","c":"_Flaggy_Rate/HR","d":"%_Bonus_Construct_EXP","e":"%_Build_Rate","f":"%_Player_Construct_XP","g":"%_Flaggy_Rate","h":"_","i":"_","j":"%_Speed_to_Flags","k":"%_Nothing_sowwy"};

export const COG_TIER_BASE_COST = [30, 200, 1500, 10000];
